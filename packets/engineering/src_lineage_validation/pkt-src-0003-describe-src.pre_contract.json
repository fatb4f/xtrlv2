{
  "schema_version": "xtrl.pre_contract/v0.2",
  "packet_id": "pkt-src-0003-describe-src",
  "component": "xtrlv2",
  "repo": "xtrlv2",
  "base_ref": "origin/main",
  "mode": "NORMAL",
  "budgets": {
    "time_minutes": 60,
    "iteration_budget": 2,
    "diff_budget": { "max_files_changed": 60, "max_lines_changed": 1200 }
  },
  "constraints": {
    "clean_repo_required": true,
    "deny_repo_local_roots": [".codex", ".quint"],
    "allowed_paths": [
      "src/**",
      "control/**",
      "docs/**",
      "tools/**",
      "schemas/**",
      "packets/**",
      "README.md"
    ],
    "forbidden_paths": [".git/**"],
    "forbidden_patterns": [
      "bash\\s+-lc",
      "subprocess\\.run\\(.*shell\\s*=\\s*True",
      "os\\.system\\(",
      "eval\\(",
      "exec\\(",
      "\\|\\|\\s*true"
    ]
  },
  "actions": {
    "test": ["python", "-m", "pytest", "-q", "--junitxml", "evidence/tests.junit.xml"]
  },
  "evidence": {
    "required_files": [
      "evidence/plan.md",
      "evidence/decision.md",
      "evidence/scope.json",
      "evidence/integrity.json",
      "evidence/tests.junit.xml",
      "evidence/regression.md",
      "evidence/events.jsonl",
      "commands.log",
      "summary.md",
      "packet.json",
      "contract.json",
      "exec-prompt.md"
    ]
  },
  "plan_card": {
    "why": "Enable deterministic rehydration of current src/ state.",
    "what": "Add describe_src action emitting src_state.json + src_state.md with lineage pointers.",
    "how": "Git log -- src/ for last N, parse trailers, include pointers to snapshot artifacts.",
    "where": "tools/ + evidence/",
    "who": "controller",
    "when": "EXEC → CHECK"
  },
  "inputs": {
    "notes": [
      "Outputs: evidence/src_state.json and evidence/src_state.md.",
      "Include last N commits touching src/ and snapshot pointers with hashes."
    ]
  }
}
