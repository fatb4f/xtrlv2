{
  "schema_version": "xtrl.pre_contract/v0.2",
  "packet_id": "pkt-v2-migrate-0004-backlog-loop",
  "component": "xtrlv2",
  "repo": "xtrlv2",
  "base_ref": "origin/main",
  "mode": "NORMAL",
  "budgets": {
    "time_minutes": 90,
    "iteration_budget": 2,
    "diff_budget": { "max_files_changed": 120, "max_lines_changed": 3000 }
  },
  "constraints": {
    "clean_repo_required": true,
    "deny_repo_local_roots": [".codex", ".quint"],
    "allowed_paths": [
      "backlog/**",
      "tools/**",
      "docs/**",
      "schemas/**",
      "control/**",
      "README.md"
    ],
    "forbidden_paths": [".git/**"],
    "forbidden_patterns": [
      "subprocess\\.run\\(.*shell\\s*=\\s*True",
      "os\\.system\\(",
      "eval\\(",
      "exec\\(",
      "\\|\\|\\s*true"
    ]
  },
  "actions": {
    "test": ["python", "-m", "pytest", "-q", "--junitxml", "evidence/tests.junit.xml"]
  },
  "evidence": {
    "required_files": [
      "evidence/plan.md",
      "evidence/decision.md",
      "evidence/scope.json",
      "evidence/integrity.json",
      "evidence/tests.junit.xml",
      "evidence/regression.md",
      "evidence/events.jsonl",
      "commands.log",
      "summary.md",
      "packet.json",
      "contract.json",
      "exec-prompt.md"
    ]
  },
  "plan_card": {
    "why": "Ensure backlog-driven iteration runs entirely from xtrlv2.",
    "what": "Port backlog sync and ensure OUT_DIR snapshots + post-updates are written under $CODEX_STATE/xtrl.",
    "how": "Implement xtrlv2 backlog sync loop; write backlog snapshots under OUT_DIR/backlog/.",
    "where": "xtrlv2/backlog + tools",
    "who": "controller",
    "when": "EXEC → CHECK"
  },
  "inputs": {
    "notes": [
      "Queue-driven loop must be PRECHECK → PLAN → EXEC → CHECK → GATE → PROMOTE/ITERATE in xtrlv2 only.",
      "OUT_DIR snapshots are required for backlog state changes."
    ]
  }
}
